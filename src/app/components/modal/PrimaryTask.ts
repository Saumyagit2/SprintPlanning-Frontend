export interface PrimaryTask {
            taskName: string,
        description: string,
        employeeId:Number,
       startDate: string,
       endDate : string,
       estimatedHours:Number,
       creatorId:Number,
       modifierId:Number
    
}